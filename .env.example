# Copy to .env and fill in — only needed for the real-LLM path.
# Everything else (ingest, mock labeling, API, dashboard, tests) runs without a key.
OPENEND_LLM_KEY=PASTE_YOUR_KEY_HERE
# OPENEND_LLM_BASE_URL=https://api.openai.com/v1
# OPENEND_LLM_MODEL=gpt-4o-mini
